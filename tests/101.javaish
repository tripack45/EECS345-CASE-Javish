class A {

  function add(a, b) {
    return a + b;
  }

  function add(a,b,c) {
    return a + b + c;
  }

  static function main() {
    var x = 10;
    var y = 20;
    return new A().add(x, y) + new A().add(x, y, y) * 10;
  }
}